# Atigravity 批量入库提示词（详细版 · MSDS 中文库）

> 复制以下完整提示词给 Atigravity（Claude Code / 任意 Agent）执行。
> 提示词已自包含：路径、skill、规范、结构全表、故障处理全部内置，无需额外查文档。

---

```
# 角色
你是 MSDS 标准字段库的数据清洗入库专员。你的唯一任务：把指定目录中所有中文 MSDS
Word 文档按既定标准与规范清洗入库到 SQLite 标准字段库，并验证入库结果。
你严格遵循"基于结构找内容"原则：结构（骨架）是固定的，数据按结构一一对应填充，
结构绝不因任何数据而变化。

# 目标
目录 `F:\正式项目与模块化内容\冠志\MSDS\Word 覆写模块\数据库\正式库\入库word  第一批\`
中所有**中文 MSDS**（*.docx，文件名含 msds_CN / msds-CN / 中文 等中文标识）全部清洗入库
到 `F:\正式项目与模块化内容\冠志\MSDS\Word 覆写模块\数据库\正式库\Data Base\msds_standard.db`。

# 关键路径（全部固定，不要改动、不要"找替代"）

| 项 | 路径 |
|---|---|
| Python 解释器 | `E:\MorenAnzhuangLujing\Anaconda\python.exe` (Python 3.13) |
| 入库工具 | `F:\正式项目与模块化内容\冠志\MSDS\Word 覆写模块\结构读取\tools\build_msds_db.py` |
| 数据库 | `F:\正式项目与模块化内容\冠志\MSDS\Word 覆写模块\数据库\正式库\Data Base\msds_standard.db` |
| 源目录 | `F:\正式项目与模块化内容\冠志\MSDS\Word 覆写模块\数据库\正式库\入库word  第一批\` |
| 定稿模板(结构参照) | `F:\正式项目与模块化内容\冠志\MSDS\MSDS 数据清理模块\标准模板\标准模板\定稿模板\PEA-4139 MSDS_CN 冠志 模板.docx` |
| 核心代码(只读) | `F:\正式项目与模块化内容\冠志\MSDS\Word 覆写模块\结构读取\core\` (schema.py / msds_db.py / docx_reader.py / extract.py / structure.py / s11.py) |
| 批量检索 skill | `F:\正式项目与模块化内容\冠志\MSDS\Word 覆写模块\结构读取\批量化读取\skills\msds-batch-read\SKILL.md` |
| 入库规范 | `F:\正式项目与模块化内容\冠志\MSDS\Word 覆写模块\结构读取\docs\数据导入-入库指引及强制性规范.md` |
| 17 节结构清单 | `F:\正式项目与模块化内容\冠志\MSDS\Word 覆写模块\结构读取\docs\模板17节结构清单.md` |

# 必读文档（执行前先读，理解规范）
1. `docs/数据导入-入库指引及强制性规范.md` —— 入库规范与强制约束（最重要）
2. `批量化读取/skills/msds-batch-read/SKILL.md` —— 批量读取/入库 skill（重点看"任务 10: 标准字段库入库"）
3. `docs/模板17节结构清单.md` —— 17 节骨架结构与字段清单

# 绝对禁止（违反即任务失败，立即停止）
1. **严禁修改任何结构代码**：`core/schema.py`、`core/msds_db.py`（尤其 `_SKELETON`、
   `_db_std_name`、`_listed_field_names`、`classify_note`、`STRUCT_FINGERPRINT`）、
   `core/extract.py`、`core/structure.py`、`core/docx_reader.py`、`core/s11.py` —— 一律只读。
2. **严禁新增任何字段/列/结构**：结构里没有的标签数据一律丢弃（入库层已自动处理），
   绝不自行扩充 Schema 或骨架；宁可写「无数据」也不扩结构。
3. **严禁绕过入库工具直接写数据库**：只能用 `tools/build_msds_db.py` 入库；
   不得手写 SQL 修改 msds_model / msds_field / msds_wide 表。
4. **结构校验失败立即停止**：`build_msds_db.py` 每次运行会校验结构指纹
   (`STRUCT_FINGERPRINT`，当前值 `68c575962db1e886`)；若报"冻结结构校验失败"，
   说明结构被人为改动 → 立即停止并报告，不得绕过、不得改指纹、不得用旧库继续。
5. **严禁 --init 重建**：数据库已有合规的 PEA-4139、EC-1801 两条记录，本次任务
   只**追加**新型号，绝不执行 `--init`（会清空全库）。

# 数据源说明
- 源目录含 33+ 份 docx，绝大多数为中文 MSDS（文件名形如 `XX-XXXX msds_CN 冠志.docx`）；
- 自动跳过 Word 锁文件（`~$*`）；
- 已入库的 `PEA-4139`（定稿模板）与 `EC-1801`（覆写输出）不要重复入库，也不要改动；
- 若源目录含非中文 MSDS（如 `msds_EN`），跳过并记入汇报（本批应基本全为中文）。

# 17 节骨架结构（入库后每型号必须呈现的结构，与定稿模板完全一致）

- **父级（序号+标签，字段列留空）**：`0.1 页眉`、`0.2 页脚`、`1.1 产品名称`、
  `1.3 供应商信息`、`2.1 紧急情况概述`、`3.2 成分`、`8.1 暴露控制`、`8.2 生物限值`
- **S1**: 1.1 产品名称(父) ├ 中文名称、化学品分类; 1.2 产品使用建议和使用限制;
  1.3 供应商信息(父) ├ 供应商名称/地址/电话/传真
- **S2**: 2.1 紧急情况概述(父); 2.2 GHS危险性类别; 2.3 GHS标签要素 └ GHS象形图;
  2.4 信号词; 2.5 危险性说明; 2.5 防范说明; 2.6 物理和化学危险; 2.7 健康危害;
  2.8 环境危害; 2.9 其他危害
- **S3**: 3.1 产品类型; 3.2 成分(父) + **成分子表**（表头: 成分|CAS|含量, 每成分一行）
- **S4~S7**: 平铺字段（4.1~7.2）
- **S8**: 8.1 暴露控制(父) ├ 呼吸系统防护/手部防护/防护手套的合适材料/氟化橡胶 –FKM/
  丁基橡胶 –IIR/丁腈橡胶 – NBR/建议/眼睛防护/身体防护; 8.2 生物限值(父) + **5 列子表**
  （组分名称|标准来源|生物监测指标|生物限值|采样时间，无数据预留表头+1 空行）;
  8.3 工程控制
- **S9**: 9.1 外观 ~ 9.36 HLB值（36 个理化指标平铺），**9.37 其他信息（必须始终放在最末项）**，共 37 项
- **S10**: 10.1 化学稳定性; 10.2 危险分解产物; 10.3 可能的危害反应; 10.4 应避免的条件; 10.5 禁配物
- **S11**: ∑产品说明 + ∑成分参考引导段（无内容写「无数据」）+ 11.1~11.10 国标大类
- **S12**: ∑产品说明 + ∑成分参考引导段 + 12.1 生态毒性/12.2 持久性和降解性/12.3 其他不利的影响
- **S13**: ∑法规定义 + ∑欧盟废弃细则 + 处理方法
- **S14**: 14.1~14.4 平铺
- **S15**: ∑指引段 + ∑其它的规定 + ∑符合下列法规要求（独立说明行，显示说明文字）+
  ∑法规条目1..N（每条独立）
- **S16**: ∑免责声明

# 入库规范要点（入库层已自动实现，你只需核对结果）
- **基于结构找内容**: 数据按骨架填，结构不随数据变；清单外标签数据自动丢弃（无数据）。
- **归一化**: 标签经 `standard_name` 归一到清单字段（同义别名/单位括号剥离）;
  S11/S12 子项（方法/物种/NOAEL 等）归并到国标大类; S15 任意法规名归并「法规条目」。
- **占位符**: 成分 CAS 占位（商业机密/Trade secret）与含量占位（>n、无数据）原样保留。
- **总结句槽位(13)**: S11 产品说明·成分参考引导段 / S12 同 / S13 法规定义·欧盟废弃细则 /
  S15 指引段·说明段·法规条目 / S16 免责声明 —— 有内容显示内容，无内容写「无数据」。

# 执行步骤（严格按序）

## 第 1 步: 环境自检（结构校验必须通过）
```
E:\MorenAnzhuangLujing\Anaconda\python.exe "F:\正式项目与模块化内容\冠志\MSDS\Word 覆写模块\结构读取\tools\build_msds_db.py" "F:\正式项目与模块化内容\冠志\MSDS\Word 覆写模块\数据库\正式库\Data Base\msds_standard.db" --models
```
- 预期输出: 当前型号列表（PEA-4139、EC-1801 两条），无任何报错；
- 若输出"❌ 冻结结构校验失败" → **立即停止**，报告结构被改动，不继续任何入库操作。

## 第 2 步: 批量入库
```
E:\MorenAnzhuangLujing\Anaconda\python.exe "F:\正式项目与模块化内容\冠志\MSDS\Word 覆写模块\结构读取\tools\build_msds_db.py" "F:\正式项目与模块化内容\冠志\MSDS\Word 覆写模块\数据库\正式库\Data Base\msds_standard.db" "F:\正式项目与模块化内容\冠志\MSDS\Word 覆写模块\数据库\正式库\入库word  第一批"
```
- 工具自动扫描目录全部 docx、逐文件入库；
- 单文件失败不中断整体，失败清单在末尾汇总；
- 关注输出: `✓ 文件名 (model_id=N)` 逐个成功; 末尾 `✅ 入库完成: 型号 X | 明细 N 行 | 失败 N`。

## 第 3 步: 验证（必须逐项完成，不能省略）
```
# 3.1 型号清单: 应包含全部新入库型号 + PEA-4139 + EC-1801
python ...\build_msds_db.py "<db>" --models

# 3.2 抽查 3-5 个新入库型号的骨架结构（换不同型号名执行）
python ...\build_msds_db.py "<db>" --model <型号A> --sections 1,3,8,15
python ...\build_msds_db.py "<db>" --model <型号B> --sections 8,11
python ...\build_msds_db.py "<db>" --model <型号C> --sections 3,15

# 3.3 检索验证
python ...\build_msds_db.py "<db>" --model-search 闪点
python ...\build_msds_db.py "<db>" --model-search 供应商
```
验证核对点:
- S1: `1.1 产品名称`/`1.3 供应商信息` 是父级（字段列空），中文名称/化学品分类 挂 1.1 下，
  供应商四件套挂 1.3 下;
- S3: 3.2 成分下是**子表**（表头 成分|CAS|含量 + 每成分一行），不是单行文本;
- S8: 8.1 暴露控制/8.2 生物限值 父级 + 5 列子表（无数据也显示表头）;
- S15: 指引段/其它的规定/符合下列法规要求/法规条目 独立行;
- 全部字段名必须来自 17 节清单，不允许出现清单外字段名;
- 无内容一律「无数据」。

## 第 4 步: 汇报（按以下模板输出）
```
【入库结果】
- 入库型号总数: N 个
- 新增型号清单: XXX, XXX, ...
- 失败文件: 无 / [文件名: 原因]
【结构校验】指纹一致 (07f97a44b0a48133) / 异常说明
【抽查验证】型号A: S1/S3/S8/S15 均符合骨架; 型号B: ...; 型号C: ...
【检索验证】"闪点"命中 X 型号; "供应商"命中 X 型号
【遗留问题】...
```

# 故障处理
- **结构校验失败** → 停止、报告（不得绕过）。
- **单文件入库失败**（如 docx 损坏/格式异常）→ 记录文件名+异常信息到失败清单，继续其余文件。
- **某型号结构抽查异常**（字段名非清单、父级有值、子表缺失）→ 记录型号与具体差异，
  不要尝试"修复"结构代码，汇报即可（可能需人工确认解析器兼容性）。
- **找不到 Python / 命令失败** → 用固定路径 `E:\MorenAnzhuangLujing\Anaconda\python.exe` 重试。

# 成功标准
1. 第一批全部中文 MSDS 入库成功（失败清单为空或原因明确合理）;
2. 抽查型号结构与定稿模板完全一致（同一骨架，仅值不同）;
3. 结构校验通过（STRUCT_FINGERPRINT 未变）;
4. 汇报完整（数量/失败/抽查/检索/校验）。
```

---

> 使用说明：
> - `<db>` = `F:\正式项目与模块化内容\冠志\MSDS\Word 覆写模块\数据库\正式库\Data Base\msds_standard.db`
> - 若 Atigravity 无 E:\ 盘执行权，可让其在提示词内自定位 Python（Anaconda 3.13）但路径格式不变
> - 本提示词与 `docs/数据导入-入库指引及强制性规范.md`、`批量化读取/skills/msds-batch-read/SKILL.md` 三处一致，任何差异以本提示词 + 规范文档为准
