# MSDS 推导产出系统 — 项目全功能 PRD

> **文档版本**: v1.0
> **项目名称**: 冠志 MSDS 推导产出系统
> **文档性质**: 产品需求文档（PRD）/ 全功能需求规格说明书
> **编写日期**: 2026-08-14
> **目标读者**: 产品 / 研发 / 测试 / 数据团队

---

## 1. 项目概述

### 1.1 背景与痛点

冠志（及国彩）化工企业需要持续向客户、法规机构、供应链提供符合规范的 MSDS（化学品安全技术说明书，Material Safety Data Sheet / SDS）。当前痛点：

1. **存量 MSDS 质量参差**：大量历史 MSDS 不符合 GB/T 16483-2008 / GB/T 17519-2013 规范（缺节、错位、值混入标签、缺 H/P 代码、引用过时法规等），实测 766 份文件中仅 6 份"完美合格"。
2. **人工重写成本高**：一份 MSDS 的规范重写依赖专业人员的法规知识与成分危害数据，耗时数小时。
3. **数据分散**：成分危害数据、法规原文、历史 MSDS、CAS 物质信息分散在 Excel/PDF/网页中，无统一数据库支撑。
4. **多格式产出**：同一产品需产出 中文/英文 × 冠志/国彩 × Word/PDF 多种规格，人工重复排版。
5. **推导结果不可追溯**：AI 推导缺乏"唯一事实源 + 真实源"的双源约束与过程记录。

### 1.2 项目目标

构建一个**"表单采集 → 检索诊断 → 数据库支撑 → 联网搜证 → AI 推导 → 模板覆写 → 多格式输出"**的 MSDS 全链路推导产出系统：

- **输入**：用户表单（S1/S3/S9 源头数据）或存量 MSDS 文件
- **过程**：以【事实原】（历史 MSDS/法规/CAS 库）为唯一事实源，以【真实源】（Chrome 插件搜集的权威渠道数据）为外部验证源，由 AI Agent 依据 GB/T 16483/17519 推导其余各节
- **输出**：符合规范、可追溯、多语言多模板多格式的 MSDS 成品

### 1.3 成功指标（KPI）

| 指标 | 目标 |
|---|---|
| 存量 MSDS 规范合格率 | 从 ~1% 提升至 ≥90% |
| 单份 MSDS 推导产出时长 | 从人工数小时 → AI 辅助 ≤30 分钟 |
| 推导内容可追溯率 | 100%（每条推导结论标注依据来源） |
| 多格式转换 | 1 份源 → 8 种规格（中/英 × 冠志/国彩 × Word/PDF） |
| 数据落地率 | 成分危害 / 法规 / CAS 库结构化入库 ≥95% |

---

## 2. 系统总体架构

```
┌─────────────────────────────────────────────────────────────┐
│                        用户入口层                            │
│   表单系统(GUI)  │  检索系统(GUI/CLI)  │  批量任务(CLI)       │
└──────────────┬──────────────────────────────────────────────┘
               │
┌──────────────▼──────────────────────────────────────────────┐
│                        核心流程编排                          │
│  读取/检索 → 规范诊断 → 推导(Agent) → 覆写 → 格式转换 → 校验  │
└──────┬───────────────┬───────────────┬─────────────────────┘
       │               │               │
┌──────▼──────┐ ┌──────▼──────┐ ┌──────▼─────────────────────┐
│ 【事实原】    │ │ 【真实源】    │ │      推导引擎 AI Agent        │
│ 数据库层      │ │ 数据搜集插件  │ │  GB 判据 + 危害库 + 浓度限值  │
│ ·历史MSDS库   │ │ ·Chrome插件  │ │  联网检索 → 逐节推导 → 依据  │
│ ·法规库      │ │ ·权威渠道    │ │  标注 (每个结论可追溯)        │
│ ·CAS清单库   │ │ (国标/部委/  │ │                            │
│ ·成分危害库   │ │  国际机构)   │ │                            │
└─────────────┘ └─────────────┘ └────────────────────────────┘
               │
┌──────────────▼──────────────────────────────────────────────┐
│                        产出层                               │
│  覆写模块(套模板) → 格式转换(语言×品牌×格式) → 交付/归档        │
└─────────────────────────────────────────────────────────────┘
```

### 2.1 核心概念定义

| 概念 | 定义 |
|---|---|
| **事实原（Fact Source）** | 系统内已落库的可信数据：历史 MSDS、法规原文、CAS 物质清单、成分危害库、标准模板。作为推导的**唯一事实基准**，不可被外部来源覆盖 |
| **真实源（Truth Source）** | 通过数据搜集插件从权威外部渠道（国家标准全文公开系统、生态环境部、应急管理部、NITE、ECHA、PubChem 等）实时获取的**验证性数据**，用于补充/校验事实原 |
| **标准结构范式** | 以标准字段库（Excel/JSON）定义的「节 → 父子级 → 标签」为唯一输出结构；标准库有而文件没有的字段标注「无数据」 |
| **推导依据** | 每条 AI 推导结论必须挂接的支撑：法规条款 / 浓度判据 / 危害库条目 / 检索来源 URL，形成可追溯链 |

---

## 3. 功能模块详细需求

### 3.1 模块一：表单系统

**定位**：用户录入源头数据（S1/S3/S9），生成覆写引擎可消费的"写入项"。

**现状基础**：`结构读取/表单系统与推断引擎/`（core/form_schema.py + gui/form_window.py，参照 PEA-4139 表单）。

#### 3.1.1 功能需求

| 编号 | 需求 | 优先级 | 说明 |
|---|---|---|---|
| FORM-01 | S1 物料及供应商标识录入 | P0 | 8 字段：产品名称/中文名称/化学品分类/使用建议/供应商名称/地址/电话/传真 |
| FORM-02 | S3 成分/组成资料录入 | P0 | 产品类型下拉（混合物/单质化合物/未知）+ 成分表增删行（名称/CAS/含量%） |
| FORM-03 | S9 物理化学特性录入 | P0 | 22 字段动态表格，增删行自动重排 9.1~9.N |
| FORM-04 | 生成写入项 JSON | P0 | 输出 `{sections:{0,1,3,9}, keep_structure:[2], empty_policy:"warn"}`，符合覆写引擎契约 |
| FORM-05 | S0 页眉页脚自动派生 | P0 | 由 S1 产品名称/中文名称自动生成产品名称/产品型号字段 |
| FORM-06 | 必填校验 | P1 | S3 产品类型必选；成分行三者至少填一项；空行自动跳过 |
| FORM-07 | 表单模板记忆 | P2 | 记住上次填写内容（同产品重复录入）；供应商信息可保存为常用档案 |
| FORM-08 | 批量录入 | P2 | 支持从 Excel 批量导入 S1/S3/S9 数据生成多个写入项 |

#### 3.1.2 数据契约

```json
{
  "sections": {
    "0": [{"seq": "", "label": "产品名称", "value": "PEA-4139"}],
    "1": [{"seq": "1.1", "label": "中文名称", "value": "..."}],
    "3": {"产品类型": "混合物", "components": [{"name":"...", "cas":"...", "conc":"..."}]},
    "9": [{"seq": "9.1", "label": "外观", "value": "乳白色液体"}]
  },
  "keep_structure": [2],
  "empty_policy": "warn"
}
```

---

### 3.2 模块二：检索系统（导入 MSDS → 检索与输出）

**定位**：解析任意 MSDS，按**标准结构范式**输出，支持批量检索与规范诊断。

**现状基础**：`结构读取/`（core/docx_reader.py 等，全库 646+ 文件解析 0 失败、token 级 0 遗漏）。

#### 3.2.1 功能需求

| 编号 | 需求 | 优先级 | 说明 |
|---|---|---|---|
| RET-01 | MSDS 文件读取 | P0 | 支持 .docx/.doc；兼容段落式/表格式/合并单元格/VML 文本框/自动编号 |
| RET-02 | 16 节三级父子级解析 | P0 | 节 → 大标题/小标题 → 标签 → 字段（GB/T 16483 结构） |
| RET-03 | 页眉页脚 → Section 0 字段化 | P0 | 页码/版本/公司名/产品名等可编辑状态标注 |
| RET-04 | S3 成分表多形态解析 | P0 | 三列表/单列表格/列内多行/'Mixtures' 声明；全角转半角归一化 |
| RET-05 | S8.2 生物限值子表 | P1 | 组分名称/标准来源/监测指标/限值/采样时间结构化 |
| RET-06 | 关键词检索 | P0 | 树上检索保留父子级；scope: label/value/all/section；空格多词 AND；页码排除 |
| RET-07 | **标准范式输出** | P0 | **以标准字段库（父级→标签）为唯一输出范式**；标准库有而文件无 → 自动标「无数据」；同父级多值 → 自动扩列（成分1/2/3…） |
| RET-08 | 批量检索矩阵 | P0 | 多文件 → 型号×标准字段 对比矩阵（无数据黄色标注），定位需重构文件 |
| RET-09 | 规范符合性诊断 | P0 | 逐字段对照标准库：命中/缺失/多余；父子级对照；输出诊断报告 |
| RET-10 | 导出 | P0 | JSON（标准范式）/ TSV / Excel 矩阵 / Word 结构文档 |
| RET-11 | 检索不命中页码 | P1 | Section 0 页码字段不参与检索（已实现） |
| RET-12 | 标准字段库同步 | P1 | 标准库 Excel 变更 → 一键重新提取标准结构 JSON |

#### 3.2.2 标准范式输出格式（核心）

```
行1: 父级分组（合并单元格）  0.1 页眉 | 0.2 页脚 | 1.1 产品名称 | 1.3 供应商信息 ...
行2: 标签                Version | 产品名称 | 中文名称 | 化学品名称（1）| CAS编号（1）...
行3+: 数据（每文件一行）     PEA-4139 | V1.0 | ... | 无数据(黄底) | ...
```

规则：
- 标准库有、文件未检出 → `无数据`（matched=false）
- 同一父级标签多值 → 扩列 `标签(2)(3)`，成分槽 1~5+ 动态展开
- 锚点列（父级占位）不取值不标无数据
- 同义归一化：GHS象形图←象形图、危险性说明←危害性说明（schema.standard_name）

---

### 3.3 模块三：数据库（事实原）

**定位**：MSDS 全生命周期数据的唯一事实源，支撑检索/推导/覆写。

**现状基础**：`数据库/`（MSDS 中文/英文/冠志/国彩 分类）、`数据库与推断引擎/法规匹配库/`（法规 PDF + JSON 索引）、`结构读取/三表建库`（标准汇总表+原始表+字段映射表）。

#### 3.3.1 数据域规划

| 数据库 | 内容 | 现状 | 目标 |
|---|---|---|---|
| **MSDS 历史库** | 全部历史 MSDS 的结构化解析结果 | Excel 三表库 | SQLite 表：文件元信息 + 每字段记录（含 值/无数据/来源文件） |
| **法规库** | 法律法规/国家标准/行业标准 | 法规匹配库（PDF+JSON 索引） | SQLite：法规条目（编号/名称/现行版本/正文路径/要点摘要） |
| **CAS 物质清单库** | 物质/CAS 号/危险分类/危害数据 | 部分 JSON | SQLite：物质主档 + 危害分类 + 理化 + 毒理 + 生态 + 限值 |
| **成分危害速查库** | 常用成分 GHS 分类/限值 | hazards_lib.md | SQLite + 保留 Markdown 编辑源 |
| **标准模板库** | 冠志/国彩 × 中/英 × Word/PDF 模板 | 标准模板目录 | 模板元数据表（品牌/语言/格式/适用版本） |
| **标准字段库** | 父子级→标签 规范 | Excel.xlsx + 标准字段结构.json | 作为范式源头，双向同步 |

#### 3.3.2 核心表设计（SQLite）

```sql
-- MSDS 文件
CREATE TABLE msds_files (
  id INTEGER PRIMARY KEY,
  model TEXT,                -- 型号 (S1 产品名称)
  file_path TEXT UNIQUE,
  brand TEXT,                -- 冠志/国彩
  language TEXT,             -- 中文/英文
  sha256 TEXT,
  sections_found INTEGER,
  missing_sections TEXT,     -- 缺失节 JSON
  imported_at TEXT
);

-- MSDS 字段记录 (检索/标准范式结果)
CREATE TABLE msds_records (
  id INTEGER PRIMARY KEY,
  file_id INTEGER REFERENCES msds_files(id),
  section INTEGER,           -- 0..16
  parent TEXT,               -- 父级
  label TEXT,                -- 标签 (标准字段)
  seq TEXT,                  -- 序号
  value TEXT,                -- 字段值
  status TEXT,               -- 'matched' | '无数据' | 'anchor'
  extra_idx INTEGER DEFAULT 1,  -- 多值扩列序号
  UNIQUE(file_id, section, parent, label, extra_idx)
);

-- 法规
CREATE TABLE regulations (
  id INTEGER PRIMARY KEY,
  code TEXT,                 -- 标准/法规编号 GB/T 16483
  title TEXT,
  category TEXT,             -- 法律/行政法规/国标/行业标准/国际公约
  is_current INTEGER,        -- 是否现行有效
  doc_path TEXT,
  summary TEXT
);

-- CAS 物质
CREATE TABLE cas_substances (
  cas TEXT PRIMARY KEY,
  name_zh TEXT, name_en TEXT,
  ghs_class TEXT,            -- GHS 分类
  oel_cn TEXT,               -- 中国职业接触限值 GBZ 2.1
  ld50 TEXT, ec50 TEXT,
  hazard_statements TEXT,    -- H 码
  pictograms TEXT
);
```

#### 3.3.3 功能需求

| 编号 | 需求 | 优先级 |
|---|---|---|
| DB-01 | 历史 MSDS 批量入库（检索结果落库） | P0 |
| DB-02 | 法规库结构化（编号/版本/正文/要点） | P0 |
| DB-03 | CAS 物质清单库 | P0 |
| DB-04 | 成分危害速查库（与 hazards_lib.md 双向同步） | P1 |
| DB-05 | 标准字段库 / 标准模板元数据 | P1 |
| DB-06 | SQL 查询接口（按型号/缺失字段/法规查询） | P1 |
| DB-07 | 数据溯源（每条记录指向来源文件/法规/URL） | P1 |
| DB-08 | 数据校验（CAS 格式、含量和、法规版本时效） | P2 |

---

### 3.4 模块四：数据搜集插件（真实源）

**定位**：Chrome 浏览器扩展，从权威外部渠道搜集并结构化 MSDS 推导所需的验证数据。

#### 3.4.1 目标信息渠道（权威源）

| 类别 | 渠道 |
|---|---|
| 中国法规标准 | 国家标准全文公开系统、生态环境部（MEE）、应急管理部、卫健委 |
| 危险化学品目录 | 《危险化学品目录》2015 版及调整公告 |
| GHS 分类 | GB 30000 系列、GHS Rev.10 |
| 职业接触限值 | GBZ 2.1-2019（优先）、ACGIH TLV |
| 国际物质数据 | ECHA（REACH）、PubChem、NITE（日本） |
| 象形图 | GHS01~GHS09、TDG 运输标志 |

#### 3.4.2 功能需求

| 编号 | 需求 | 优先级 | 说明 |
|---|---|---|---|
| PLG-01 | 页面数据抓取 | P0 | 在当前网页提取结构化数据（表格/字段） |
| PLG-02 | 站点模板适配 | P0 | 为常用权威站点配置解析模板（选择器→字段映射） |
| PLG-03 | 数据标准化 | P0 | 抓取结果 → 统一 schema（CAS/分类/H码/限值/来源URL） |
| PLG-04 | 一键存入数据库 | P0 | 校验后写入对应表（cas_substances/regulations） |
| PLG-05 | 来源留痕 | P0 | 自动记录 URL/抓取时间/站点名称 |
| PLG-06 | 重复检测 | P1 | 同 CAS/法规编号已存在 → 提示更新或跳过 |
| PLG-07 | 批量抓取辅助 | P2 | 目录页批量导航/翻页抓取 |
| PLG-08 | 手动修正入口 | P1 | 抓取数据可人工编辑确认后入库 |

#### 3.4.3 抓取数据 Schema（统一格式）

```json
{
  "source_url": "https://...",
  "source_name": "ECHA",
  "fetched_at": "2026-08-14T10:00:00",
  "substance": {"cas": "112-34-5", "name_en": "2-Butoxyethanol"},
  "classifications": [{"hazard": "Flam. Liq. 3", "h_code": "H226", "cat": "类别3"}],
  "limits": [{"type": "OEL", "region": "CN", "std": "GBZ 2.1", "value": "100 mg/m3"}],
  "toxicity": [{"endpoint": "LD50 oral", "value": "3306 mg/kg"}],
  "raw_text": "..."
}
```

---

### 3.5 模块五：推导引擎（AI Agent）

**定位**：基于【事实原 + 真实源】双源约束，由 AI Agent 依据 GB/T 16483-2008 / GB/T 17519-2013 从 S1/S3/S9 推导其余各节（S2、S4~S8、S10~S16）。

**现状基础**：`数据库与推断引擎/判断skill/`（msds-inference-write 技能：msds_reader → build_initial_content → 联网检索完善 → msds_docx 产出）。

#### 3.5.1 推导总逻辑（判据链）

```
S1/S3/S9 既定事实
    │
    ├─ 可燃性判据: S9 闪点 + S3 可燃成分浓度
    │     → S2 物理危险 / S5 消防 / S7 储存 / S14 运输
    ├─ 健康危害判据: 危险成分含量 vs 浓度限值
    │     (急性≥1% / STOT≥1% / 腐蚀刺激≥1% / 过敏·致癌·生殖≥0.1% / 吸入危害≥10%+黏度)
    │     → S2 健康危害 / S4 急救 / S8 接触控制 / S11 毒理
    ├─ 环境危害判据: 水生危害成分 (水环境≥1%)
    │     → S2 环境危害 / S6 泄漏 / S12 生态 / S13 废弃
    └─ 法规引用: S15 现行版法规 / S16 编制依据
```

#### 3.5.2 功能需求

| 编号 | 需求 | 优先级 | 说明 |
|---|---|---|---|
| INF-01 | 事实提取 | P0 | 读取源 MSDS S1/S3/S9 → facts.json（复用检索系统解析） |
| INF-02 | 初稿骨架生成 | P0 | 16 节全量 content.json（既定事实带入 + 其余节"待推导"占位） |
| INF-03 | 联网检索补全 | P0 | 未收录成分 → web 检索 GHS 分类/H码/限值/毒理/生态/目录状态 |
| INF-04 | 双源约束 | P0 | 事实原优先，真实源校验；冲突时以事实原为准并记录差异 |
| INF-05 | 逐节推导 | P0 | 按判据链推导，每节从"待推导"完善为规范内容 |
| INF-06 | 依据标注 | P0 | 每条推导结论挂接：法规条款/浓度判据/危害库/检索URL |
| INF-07 | 16 节铁律检查 | P0 | 高频铁律表（S4 眼睛冲洗15min、S8 必引GBZ 2.1、S11 禁混入无关毒理数据等） |
| INF-08 | 浓度限值判据 | P0 | limits.md 表驱动（含区间保守/确定双策略、保密组分、黏度条件） |
| INF-09 | 成分危害速查 | P0 | hazards_lib.md 命中优先，未收录才联网 |
| INF-10 | 产出方案 Word | P0 | 封面/诊断汇总表/推导依据/16节（原文问题+规范应写内容）+ 灰色【说明】 |
| INF-11 | 产出纯 MSDS 正文 | P0 | 只抽可写入内容，标准表格结构，象形图实图嵌入 |
| INF-12 | 人工确认环节 | P1 | 推导结果逐节人工确认/修改后再进入覆写 |
| INF-13 | 中间产物保留 | P1 | facts.json / content.json 保留供复盘打磨 |

#### 3.5.3 推导输出 content.json 结构（节选）

```json
{
  "product": "OS-1330",
  "diagnosis": [{"section": 2, "issue": "缺 H 代码", "severity": "high"}],
  "sections": {
    "2": [
      {"t": "text", "content": "易燃液体，类别4（H227）"},
      {"t": "pictogram", "code": "GHS02"},
      {"t": "note", "content": "推导依据: S9闪点76℃ + 含量≥1% → GB 30000.7"}
    ]
  }
}
```

---

### 3.6 模块六：覆写模块

**定位**：基于固定 MSDS 标准结构（父子级→标签）与标准模板，将推导/表单结果覆写到模板副本，产出新 MSDS 文件。

**现状基础**：`覆写引擎/`（msds_overwrite_engine.py ~93KB、make_write_items.py、field_maps.json、模板字节级一致）。

#### 3.6.1 功能需求

| 编号 | 需求 | 优先级 | 说明 |
|---|---|---|---|
| OVR-01 | 写入项解析 | P0 | 读取写入项 JSON（表单/推导产出），逐字段映射 |
| OVR-02 | 字段映射 | P0 | 写入项标签 → 模板字段（field_maps.json 别名映射，可扩展多模板） |
| OVR-03 | 模板加载 | P0 | 标准模板（PEA-4139 冠志等）字节级内化，覆写不破坏布局 |
| OVR-04 | 三态覆写操作 | P0 | update（覆写值）/ insert（新增行）/ delete（显式删除） |
| OVR-05 | 保留结构控制 | P0 | keep_structure（如 S2 保持模板结构）防止误改 |
| OVR-06 | 空值策略 | P0 | empty_policy: warn/clear/keep（模板留空/清空/保留） |
| OVR-07 | 可编辑性约束 | P0 | 序号/标签列锁定，仅字段列可覆写；固定字段（页码等）不可覆写 |
| OVR-08 | 覆写日志 | P0 | 记录每步操作（update/insert/delete, 行号, 新值） |
| OVR-09 | 覆写校验 | P1 | 输出后重读比对：标签匹配率、值一致性、结构完整性 |
| OVR-10 | S0 页眉页脚覆写 | P1 | 产品名称/型号/修订日期等可变字段 |
| OVR-11 | S3 成分表重建 | P1 | 按成分列表重建模板成分表行（含保密成分策略） |
| OVR-12 | 多模板适配 | P1 | 冠志/国彩模板 field_map 分离，可切换 |

#### 3.6.2 覆写操作语义

```json
// 写入项中的操作类型
{"seq": "9.3", "label": "pH值", "value": "7-9"}                // update: 覆写模板同标签字段
{"label": "新增字段", "value": "x", "after": "锚点"}            // insert: 在锚点后新增行
{"label": "删除字段", "delete": true}                          // delete: 删除模板行
{"label": "不可编辑字段", "editable": false}                   // 锁定: 不参与覆写
```

---

### 3.7 模块七：格式转换

**定位**：将一份 MSDS 内容转换为多种规格：中文/英文 × 冠志/国彩 × Word/PDF。

#### 3.7.1 组合矩阵

| 维度 | 取值 |
|---|---|
| 语言 | 中文 / 英文 |
| 品牌模板 | 冠志 / 国彩 |
| 格式 | Word (.docx) / PDF |
| 内容形态 | 方案版（含诊断/依据）/ 纯 MSDS 版 / 标准写入版 |

合计主要规格：**中×冠志×Word、中×国彩×Word、英×冠志×Word、英×国彩×Word 及对应 PDF（8 种主规格）**。

#### 3.7.2 功能需求

| 编号 | 需求 | 优先级 | 说明 |
|---|---|---|---|
| FMT-01 | Word 渲染 | P0 | python-docx 按模板表格结构渲染（16 节 2 列表格、封面、页眉页脚、S3 成分子表、象形图实图） |
| FMT-02 | PDF 转换 | P0 | Word → PDF（LibreOffice 无头转换 / docx2pdf） |
| FMT-03 | 中英双语 | P0 | 英文字段名映射（SDS 英文模板字段表） |
| FMT-04 | 品牌模板适配 | P0 | 冠志/国彩 页眉页脚/封面/字体/配色差异 |
| FMT-05 | 批量转换 | P1 | 多产品 × 多规格批量队列 |
| FMT-06 | 转换校验 | P1 | 页数/节完整性/无缺字（字体嵌入）检查 |
| FMT-07 | 命名规范 | P0 | `{型号} msds_{语言} {品牌}.docx/pdf` |

#### 3.7.3 输出命名规范

```
{型号} msds_CN 冠志.docx / {型号} msds_EN Guanzhi.docx
{型号} msds_CN 国彩.docx / {型号} msds_EN Guocai.docx
（PDF 同理）
```

---

## 4. 端到端业务流程

### 4.1 主流程：存量 MSDS 规范化重写

```
① 检索系统: 导入存量 MSDS → 标准范式输出 → 规范诊断 (缺哪些节/字段)
      │
      ▼
② 推导引擎: 提取 S1/S3/S9 事实 → 联网检索真实源 → 依据判据推导其余节
      │        → 产出 方案Word + 纯MSDS正文 + content.json
      ▼
③ 人工确认: 逐节确认推导内容 (修改/批准)
      │
      ▼
④ 覆写模块: content → 写入项 → 套标准模板 → 新 MSDS docx
      │
      ▼
⑤ 格式转换: 中/英 × 冠志/国彩 × Word/PDF 批量产出
      │
      ▼
⑥ 校验归档: 重读比对 + 入库历史库
```

### 4.2 主流程：新产品 MSDS 创建（无存量）

```
① 表单系统: 录入 S1/S3/S9 (产品/供应商/成分/理化)
      │
      ▼
② 推导引擎: 依据表单事实 + 真实源推导 (同 4.1 ②~⑥)
```

### 4.3 支撑流程：数据建设

```
法规/CAS/成分数据 → 数据搜集插件(Chrome) 抓取 → 标准化 → 数据库(事实原)
                    → 手动录入/导入补充 → 数据库校验 → 数据版本管理
```

---

## 5. 非功能需求

| 类别 | 需求 |
|---|---|
| **性能** | 单文件解析 ≤2s；单份推导 ≤30min（含联网检索）；批量 100 份矩阵导出 ≤2min |
| **兼容性** | Windows 10/11；Python 3.11+；Chrome 插件 Manifest V3；无需 Office 安装（python-docx/openpyxl/LibreOffice 无头） |
| **可靠性** | 解析 0 失败率目标 100%（异常文件报告不中断）；token 级完整性校验；断网时推导可离线降级（仅事实原） |
| **可追溯** | 每条推导结论含依据来源；覆写日志可审计；数据入库含来源 URL/时间 |
| **安全** | 敏感数据（保密成分、供应商）权限控制；本地数据库加密可选；API 凭据不入库 |
| **可扩展** | 新法规/新成分/新模板/新品牌 无需改代码（数据驱动：规则库/字段库/模板元数据） |

---

## 6. 数据流与接口契约

### 6.1 模块间接口

| 接口 | 方向 | 契约 |
|---|---|---|
| 写入项 | 表单→覆写 | `{sections, keep_structure, empty_policy}` |
| facts.json | 检索→推导 | `{file, s1, s3, s9, sections_index}` |
| content.json | 推导→覆写/文档 | `{product, diagnosis, sections:[{t, content, code?}]}` |
| 标准字段结构 | 字段库→检索 | `{sections:[{number,title,groups:[{parent,anchor,fields}]}]}` |
| 标准范式记录 | 检索→数据库 | `{file_id, section, parent, label, value, status}` |
| 抓取数据 | 插件→数据库 | `{source_url, substance, classifications, limits}` |
| 覆写日志 | 覆写→审计 | `{op, section, row, label, old, new}` |

### 6.2 标准结构范式（全系统唯一规范）

以 `标准字段结构.json` 为唯一来源（由标准字段库 Excel 提取）：
- **17 节**（S0 页眉页脚 + S1~S16）
- **139 列**：105 字段 + 9 锚点 + 15 成分槽 + 10 通栏
- 所有模块（检索输出/推导目标/覆写校验/数据库落库）共用此范式

---

## 7. 里程碑规划

| 阶段 | 内容 | 交付物 |
|---|---|---|
| **M1 基础能力**（已完成 ~80%） | 检索系统全功能、标准范式输出、表单系统、覆写引擎单模板、判断skill 推导流程 | 单份 MSDS 全链路产出 |
| **M2 数据建设** | SQLite 数据库（历史 MSDS/法规/CAS）、数据搜集插件 v1（ECHA/国标网） | 事实原 + 真实源双库可用 |
| **M3 引擎完善** | 推导引擎 Agent 化（双源约束、依据标注、铁律自动检查）、覆写多模板 | 推导结果可追溯、多模板覆写 |
| **M4 格式矩阵** | 中英 × 冠志/国彩 × Word/PDF 批量转换 + 校验 | 8 种主规格批量产出 |
| **M5 集成验收** | 端到端批量流水线、人工确认环节、KPI 达标 | 全流程交付 + 验收报告 |

---

## 8. 验收标准（核心场景）

| 场景 | 验收条件 |
|---|---|
| 存量 MSDS 检索诊断 | 导入任意 .docx → 标准范式输出；标准库有而文件缺 → 显示「无数据」；父子级错误被检出 |
| 批量检索 | 选目录 → 型号×标准字段对比矩阵 xlsx；每文件无数据统计可定位重构目标 |
| 表单→覆写 | 表单录入 S1/S3/S9 → 写入项 → 套冠志模板 → 新 docx 值正确、结构完整 |
| 推导 | 给定 S1/S3/S9 → 16 节无占位；S2 分类与 S9 闪点一致；S8 引 GBZ 2.1；每条含依据 |
| 数据入库 | 插件抓取 CAS 数据 → 标准化入库 → 同 CAS 去重/更新；来源 URL 留痕 |
| 格式转换 | 1 份源 → 8 种规格，节完整、无缺字、命名规范 |
| 回归测试 | 检索系统 74 项测试全通过；覆写引擎关键路径测试通过 |

---

## 9. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 法规更新（如 GB 30000 改版） | 推导结论过时 | 法规库版本管理 + 现行版本标记 + 定期巡检 |
| 成分危害数据缺失/冲突 | 推导不准确 | 双源校验 + 人工确认环节 + 危害库持续扩充 |
| 保密成分（商业机密） | 无法完整推导 | 名称/CAS 可保密，危险性必须列明（限值判据仍生效） |
| 复杂模板格式（.doc 老格式） | 覆写失败 | 模板内化+格式降级处理；老格式先转 docx |
| AI 推导幻觉 | 编造数据 | 事实原优先 + 依据标注 + 16 节铁律检查 + 人工确认 |
| 网络检索受限 | 推导中断 | 多搜索源降级（360/so.com 等）+ 本地危害库兜底 |

---

## 10. 附录

### 10.1 参考标准

- GB/T 16483-2008《化学品安全技术说明书 内容和项目顺序》
- GB/T 17519-2013《化学品安全技术说明书 编写指南》
- GB 13690《化学品分类和危险性公示 通则》
- GB 30000.2-29《化学品分类和标签规范》系列
- GB 15258《化学品安全标签编写规定》
- GBZ 2.1-2019《工作场所有害因素职业接触限值》
- GHS Rev.10（紫皮书）

### 10.2 现有资产清单

| 资产 | 位置 | 说明 |
|---|---|---|
| 检索系统 | `结构读取/` | 74 项回归测试，标准范式输出已实现 |
| 覆写引擎 | `覆写引擎/msds_overwrite_engine.py` | 模板覆写核心 |
| 判断skill | `数据库与推断引擎/判断skill/` | 推导流程 + 判据 + 危害库 + 象形图 |
| 法规匹配库 | `数据库与推断引擎/法规匹配库/` | 法规 PDF + JSON 索引 + 抓取工具 |
| 标准字段库 | `数据库/正式库/标准字段数据库Excel/Excel.xlsx` | 父子级→标签 规范源头 |
| 标准模板 | `标准模板/标准模板/定稿模板/` | PEA-4139 冠志/国彩 模板 |
| 数据清单 | `MSDS 数据清理模块/标准模板/数据清单/` | 产品清单 |

### 10.3 术语表

| 术语 | 含义 |
|---|---|
| MSDS/SDS | 化学品安全技术说明书 |
| 父子级 | 节 → 大标题/小标题 → 标签 → 字段 的层级结构 |
| 标签 | 字段名称（如 致突变性） |
| 字段 | 字段内容文本 |
| 序号 | 字段编号（如 11.5） |
| 事实原 | 系统内落库可信数据（唯一事实基准） |
| 真实源 | 外部权威渠道验证数据 |
| 写入项 | 覆写引擎消费的结构化写入指令 |
